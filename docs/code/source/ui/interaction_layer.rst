=================
Interaction Layer
=================

This layer provides the buttons, controls, menus, and other elements that allow the user to interact with the game.

The interaction layer is drawn separately from the world view, sitting on top of the world view layer.

Build Menu
==========

The build menu is accessed by either long-pressing or right clicking on a tile. This creates a pie menu, extending from the cursor.